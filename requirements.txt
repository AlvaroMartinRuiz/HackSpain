# Pinned to the versions the agent has been run and scored on. Ranges let two
# laptops resolve two different stacks, which is the one failure nobody can
# debug at four in the morning. Bump deliberately, together, never mid-weekend.
fastapi==0.141.1
uvicorn[standard]==0.53.0
httpx==0.28.1
python-dotenv==1.2.3
# 14.0 renamed extra_headers to additional_headers; src/voice/stt.py handles
# both, but the Deepgram socket is the hot path and stays on a known version.
websockets==17.1
pydantic==2.13.5
# Windows ships no IANA database, and every time in this challenge is Europe/Madrid.
tzdata==2026.4
